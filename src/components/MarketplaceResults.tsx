import { Box } from '@chakra-ui/react';
import { MarketplaceGridView } from './MarketplaceGrid/MarketplaceGridView';

export const MarketplaceResults = () => {
  return (
    <Box w="full">
      <MarketplaceGridView />
    </Box>
  );
};

export default MarketplaceResults;